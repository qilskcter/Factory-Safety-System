import React from 'react';
import { useColorScheme } from 'react-native';
// Import trực tiếp component App chính từ thư mục gốc của bạn
import MainApp from '../../App'; 

export default function TabLayout() {
  // Trả về luồng giao diện chính của hệ thống Factory Safety
  return <MainApp />;
}